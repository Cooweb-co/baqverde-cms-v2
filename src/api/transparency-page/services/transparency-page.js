"use strict";

/**
 * transparency-page service
 */

const { createCoreService } = require("@strapi/strapi").factories;

module.exports = createCoreService(
  "api::transparency-page.transparency-page"
);
